<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <%-include("../shared/imports.ejs")%>
  </head>
  <body class="vh-100 blue-gradient">
    <%-include("../shared/header.ejs")%>

    <div
      class="container h-75 d-flex justify-content-center align-items-center"
    >
      <div class="card col-10 border-black">
        <div class="card-header bg-black text-white">
          <h6 class="mt-2 p-1">LISTAGEM DE MOTORISTAS</h6>
        </div>
        <div class="card-body">
          <table class="table">
            <thead>
              <tr>
                <th>Número de registro</th>
                <th>Nome</th>
                <th>CPF</th>
                <th>Telefone</th>
                <th class="text-center">Apagar</th>
              </tr>
            </thead>
            <tbody>
              <%Motoristas.forEach(function (motorista) {%> <%if (motorista.cpf)
              {%>
              <tr>
                <td><%=motorista.numRegistro%></td>
                <td><%=motorista.nome%></td>
                <td><%=motorista.cpf%></td>
                <td><%=motorista.telefone%></td>
                <td class="text-center">
                  <a
                    class="btn btn-danger"
                    href="/motorista/apagar/<%=motorista._id%>"
                    >X</a
                  >
                </td>
              </tr>
              <%}%> <%})%>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </body>
</html>
